{
  "buildCommand": "cd apps/web && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --legacy-peer-deps",
  "env": {
    "NEXT_PUBLIC_ENABLE_DEV_MENU": "true"
  },
  "ignoreCommand": "git check-ignore -q $FILE_PATH 1>/dev/null 2>&1 && echo true || git ls-files --error-unmatch $FILE_PATH 1>/dev/null 2>&1 || echo true"
}
